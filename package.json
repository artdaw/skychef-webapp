{
  "name": "skychef",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "dev": "cd packages/webapp && pnpm dev",
    "build": "cd packages/webapp && pnpm build",
    "start": "cd packages/webapp && pnpm start",
    "lint": "cd packages/webapp && pnpm lint"
  }
}
